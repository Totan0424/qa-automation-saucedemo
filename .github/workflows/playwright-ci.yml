name: Playwright Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      ACTIONS_STEP_DEBUG: true  # Habilita logs detallados en GitHub Actions

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps --force  # Fuerza la instalación para evitar fallos

      - name: Run Playwright tests
        run: npx playwright test --reporter=html

      - name: Upload test artifacts (screenshots, videos, traces)
        if: failure()
<<<<<<< HEAD
        uses: actions/upload-artifact@v2
=======
        uses: actions/upload-artifact@v3.1.2  # Usa una versión específica de upload-artifact
>>>>>>> f901dc9 (Fix GitHub Actions workflow)
        with:
          name: playwright-results
          path: |
            playwright-report
            test-results
